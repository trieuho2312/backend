openapi: 3.0.3
info:
  title: BK Platform API
  version: 0.1.0
servers:
  - url: http://localhost:8080
paths:
  /api/auth/register:
    post:
      summary: Register with HUST email
      requestBody:
        required: true
      responses:
        '200':
          description: OK
  /api/auth/login:
    post:
      summary: Login and get JWT
      responses:
        '200':
          description: OK
